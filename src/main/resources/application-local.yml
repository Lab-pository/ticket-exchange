spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/ticket_exchange"
    username: user
    password: password
  flyway:
    enabled: true
    locations: classpath:db/migration
